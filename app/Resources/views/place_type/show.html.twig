{% extends 'base.html.twig' %}

{% block body %}
    <h1>Place_type</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ place_type.id }}</td>
            </tr>
            <tr>
                <th>Pltyname</th>
                <td>{{ place_type.pltyName }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('place_type_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('place_type_edit', { 'id': place_type.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
